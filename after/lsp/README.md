# Lsp configuration

In this directory puts your configuration after loaded the initial configuration. In other words the initial configuration is loading first, then after/lsp/YOUR-LSP-CONFIG is loaded after.

# LSP configuration

In this directory puts your configuration is loaded first, and ignore the default configuration by mason-lspconfig. In other words the initial configuration is your lsp/YOUR-LSP-CONFIG.
